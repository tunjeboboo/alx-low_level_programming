#include "main.h"

/**
 * flip_bits - returns the number of bits you would
 * owned by asofeyeje
 * @n: first num
 * @m: second num
 *
 * Return: num of bits.
 */
unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	unsigned int ibits;

	for (ibits = 0; i || j; i >>= 1, j >>= 1)
	{
		if ((i & 1) != (j & 1))
		ibits++;
	}

	return (ibits);
}
